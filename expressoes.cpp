#include <stdio.h>

int main(){
	int a =10, b = 20;
	int soma = a + b;
	int sub = a - b;
	int multi = a * b;
	int div = b / a;
	int resto = b % a;
	
	
	printf ("soma: %d\n", soma);
	printf ("subtracao: %d\n", sub);
	printf ("multiplicacao: %d\n", multi);
	printf ("divisao: %d\n", div);
	printf ("resto: %d\n", resto);
	
	return 0;
}
